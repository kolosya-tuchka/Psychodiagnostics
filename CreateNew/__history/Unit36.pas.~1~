unit Unit36;

interface

uses
  WinApi.Windows, WinApi.Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, Grids, DBGrids, DB, Data.Win.ADODB;

type
  TForm36 = class(TForm)
    Label1: TLabel;
    Button1: TButton;
    procedure Button1Click(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);

  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form36: TForm36;
  nazv,question,otv1,otv2,otv3 :string;
  kolvo:integer;
  k:integer;

implementation

uses Unit34, Unit33, Unit32, Unit31, Unit30, Unit29, Unit37, Unit35,
  Unit39, Unit40, Unit41, Unit58, Unit55, Unit56, Unit57, Unit2,
  Unit62, Unit27;

{$R *.dfm}
procedure TForm36.Button1Click(Sender: TObject);
    begin
    Form62.show;
    Form36.close;

 end;


procedure TForm36.FormClose(Sender: TObject; var Action: TCloseAction);
begin
  Form62.show;


end;

end.
